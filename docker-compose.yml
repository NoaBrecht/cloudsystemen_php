version: '3.8'
services:
  php:
    build: .
    ports:
      - "80:80"
    depends_on:
      - database
  database:
    image: mysql:latest
    environment:
      - "MYSQL_ROOT_PASSWORD"
    volumes:
      - db:/data/db
volumes:
  db:
